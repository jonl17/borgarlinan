import React, { useMemo } from "react"
import Img from "gatsby-image"
import { graphql, useStaticQuery } from "gatsby"
import safeGet from "lodash.get"

const QandaImage = () => {
  return <Img></Img>
}

export default QandaImage
